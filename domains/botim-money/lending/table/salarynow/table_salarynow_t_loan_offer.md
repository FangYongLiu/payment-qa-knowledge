---
id: tbl_salarynow_t_loan_offer
object_type: Table
name: Loan offers generated for applications, including status and audit info (t_loan_offer)
aliases: [t_loan_offer, salarynow.t_loan_offer]
domain: lending
status: active
owner: Xiaopei.Yan
reviewer: Xiaopei.Yan
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: salarynow schema DDL
tags: [lending, salarynow]
sensitivity: normal
related_services: []
---

# Loan offers generated for applications, including status and audit info (t_loan_offer)

## 用途
物理表 `salarynow.t_loan_offer`,主键 `id`。Loan offers generated for applications, including status and audit info。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | Primary key · 可空 |
| `offer_no` | varchar(32) | Custom application number generated by code |
| `member_id` | varchar(20) | Reference to user_profile.member_id |
| `risk_response_id` | varchar(32) | risk response connect to this offer · 可空 |
| `max_installments` | smallint(2) | Maximum number of repayments allowed from risk response · 可空 |
| `risk_level` | varchar(20) | Risk level for the product from risk response · 可空 |
| `amount_approved` | decimal(10, 2) | Approved principal amount · 可空 |
| `status` | varchar(32) | Offer status: INITIATED, OFFERED, ACCEPTED, PARTIALLY_ACCEPTED, REJECTED, EXPIRED |
| `valid_until` | timestamp | Offer expiry · 可空 |
| `created_at` | timestamp | Offer creation time |
| `updated_at` | timestamp | Last update time |

## 主键 / 索引
- 主键:`id`
- `risk_response_id`:risk_response_id (UNIQUE)
- `idx_loan_offer_member_id`:member_id
- `uk_loan_offer_offer_no`:offer_no

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
