---
id: tbl_salarynow_t_loan
object_type: Table
name: Real loan created in the system stored here (t_loan)
aliases: [t_loan, salarynow.t_loan]
domain: lending
status: active
owner: Xiaopei.Yan
reviewer: Xiaopei.Yan
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: salarynow schema DDL
tags: [lending, salarynow]
sensitivity: normal
related_services: []
---

# Real loan created in the system stored here (t_loan)

## 用途
物理表 `salarynow.t_loan`,主键 `id`。Real loan created in the system stored here。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | Primary key · 可空 |
| `loan_no` | varchar(32) | Custom loan number generated by code |
| `member_id` | varchar(20) | Member identifier / Reference to user_profile.member_id |
| `application_id` | bigint | Related application id |
| `product_id` | bigint | Related product id |
| `irr` | decimal(10, 2) | Internal rate of return (%) |
| `loan_amount` | decimal(10, 2) | Selected loan amount · 可空 |
| `tenure` | int | Selected tenure in months · 可空 |
| `processing_fee` | decimal(10, 2) | Processing fee amount |
| `processing_fee_vat_amount` | decimal(10, 2) | Processing fee VAT amount · 可空 |
| `processing_fee_vat_percentage` | decimal(10, 2) | Processing fee VAT percentage · 可空 |
| `total_interest` | decimal(10, 2) | Total interest over the tenure |
| `disburse_amount` | decimal(10, 2) | Net disbursed amount |
| `disburse_account` | varchar(64) | Account to disburse to · 可空 |
| `repay_account` | varchar(64) | Account to collect repayments from · 可空 |
| `first_due_date` | timestamp | First scheduled due date |
| `last_due_date` | timestamp | Last scheduled due date |
| `status` | varchar(32) | ACTIVE, CLOSED, PRECLOSED, FORCE_CLOSED |
| `created_at` | timestamp | Row creation time / Application creation time |
| `updated_at` | timestamp | Last update time |

## 主键 / 索引
- 主键:`id`
- `idx_t_loan_application_id`:application_id
- `idx_t_loan_member_id`:member_id

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
