---
id: tbl_ecollect_t_purchase_order
object_type: Table
name: Purchase order table (t_purchase_order)
aliases: [t_purchase_order, ecollect.t_purchase_order]
domain: wallet
status: active
owner: qianlong.wang
reviewer: qianlong.wang
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: ecollect schema DDL
tags: [wallet, ecollect]
sensitivity: normal
related_services: []
---

# Purchase order table (t_purchase_order)

## 用途
物理表 `ecollect.t_purchase_order`,主键 `id`。Purchase order table。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint(21) | Purchase order ID, generated by IdGenerator |
| `template_id` | bigint(21) | Template ID (FK -> t_order_template) |
| `template_share_id` | bigint(21) | Share record ID (FK -> t_order_template_share), for tracking conversions · 可空 |
| `shop_id` | bigint(21) | Shop ID · 可空 |
| `buyer_id` | varchar(64) | Buyer ID (Botim User ID), null for non-Botim users · 可空 |
| `buyer_name` | varchar(100) | Buyer name, "Anonymous" for non-Botim users |
| `buyer_phone` | varchar(50) | Buyer phone number · 可空 |
| `total_amount` | decimal(10, 2) | Total order amount |
| `currency` | varchar(3) | Currency code, default AED |
| `payment_mode` | varchar(20) | Payment mode: SELF_PAY, PROXY_PAY |
| `entry_type` | varchar(20) | Entry type: PRIMARY (auto add), SECONDARY (user access) |
| `source_channel` | varchar(20) | Source channel: CONTACT, PHONE, GROUP, QR_CODE, LINK, SHOP |
| `status` | varchar(20) | Status: PENDING, PAID, CANCELLED, EXPIRED, REFUNDED |
| `last_access_time` | timestamp | Last access time (UTC), for 7-day cleanup rule (BR7) |
| `customer_note` | varchar(255) | Customer note (buyer remark for shop orders) · 可空 |
| `created_at` | timestamp | Creation time (UTC) |
| `created_by` | varchar(8) | Creator UID · 可空 |
| `paid_at` | timestamp | Payment completion time (UTC) · 可空 |
| `paid_by` | varchar(8) | Payer UID · 可空 |
| `refunded_at` | timestamp | Refund completion time (UTC) · 可空 |
| `refunded_by` | varchar(8) | Refunder UID · 可空 |
| `cancelled_at` | timestamp | Cancel time · 可空 |
| `cancelled_by` | varchar(8) | Canceller UID · 可空 |

## 主键 / 索引
- 主键:`id`
- `idx_buyer_id`:buyer_id
- `idx_last_access_time`:last_access_time
- `idx_shop_id`:shop_id
- `idx_template_id`:template_id

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
