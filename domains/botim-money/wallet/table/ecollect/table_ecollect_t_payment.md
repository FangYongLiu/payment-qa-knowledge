---
id: tbl_ecollect_t_payment
object_type: Table
name: Payment and refund table (t_payment)
aliases: [t_payment, ecollect.t_payment]
domain: wallet
status: active
owner: qianlong.wang
reviewer: qianlong.wang
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: ecollect schema DDL
tags: [wallet, ecollect]
sensitivity: normal
related_services: []
---

# Payment and refund table (t_payment)

## 用途
物理表 `ecollect.t_payment`,主键 `id`。Payment and refund table。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint(21) | Payment record ID, generated by IdGenerator |
| `biz_type` | varchar(20) | Business type: PAYMENT, REFUND |
| `type` | varchar(20) | Payment type: INCOME (buyer to seller), PAYOUT (seller to buyer for refund) |
| `order_id` | bigint(21) | Purchase order ID (FK -> t_purchase_order) |
| `source_payment_id` | bigint(21) | Source payment ID (FK -> t_payment), for refund to link original payment · 可空 |
| `owner_id` | varchar(64) | Record owner ID (Botim UID), determines whose finance list this appears in |
| `payer` | varchar(64) | Payer ID (Botim UID), who pays the money |
| `payee` | varchar(64) | Payee ID (Botim UID), who receives the money |
| `total_amount` | decimal(10, 2) | Payment amount |
| `currency` | varchar(3) | Currency code, default AED |
| `payment_method` | varchar(20) | Payment method: BOTIM_WALLET, BANK_CARD, APPLE_PAY, GOOGLE_PAY · 可空 |
| `status` | varchar(20) | Payment status: PENDING, PROCESSING, PAID, FAILED, REFUNDING, REFUNDED, REFUND_FAILED |
| `trade_order_id` | varchar(64) | Trade system order ID (from Trade cashier) · 可空 |
| `external_order_no` | varchar(64) | External order number (from payment gateway) · 可空 |
| `cashier_url` | text | Cashier URL for payment (Trade cashier URL) · 可空 |
| `remark` | varchar(500) | Remark or reason (e.g., refund reason) · 可空 |
| `paid_at` | timestamp | Payment completion time (UTC) · 可空 |
| `created_at` | timestamp | Creation time (UTC) |
| `updated_at` | timestamp | Update time (UTC) · 可空 |

## 主键 / 索引
- 主键:`id`
- `idx_order_id`:order_id
- `idx_owner_id`:owner_id
- `idx_paid_at`:paid_at
- `idx_trade_order_id`:trade_order_id

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
