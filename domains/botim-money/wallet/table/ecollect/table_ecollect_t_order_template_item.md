---
id: tbl_ecollect_t_order_template_item
object_type: Table
name: Order template item table (t_order_template_item)
aliases: [t_order_template_item, ecollect.t_order_template_item]
domain: wallet
status: active
owner: qianlong.wang
reviewer: qianlong.wang
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: ecollect schema DDL
tags: [wallet, ecollect]
sensitivity: normal
related_services: []
---

# Order template item table (t_order_template_item)

## 用途
物理表 `ecollect.t_order_template_item`,主键 `id`。Order template item table。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint(21) | Item ID, generated by IdGenerator |
| `template_id` | bigint(21) | Template ID (FK -> t_order_template) |
| `item_type` | varchar(20) | Item type: PAYMENT_ITEM, PRODUCT_ITEM |
| `product_id` | bigint(21) | Product ID (FK -> t_product), only for PRODUCT_ITEM · 可空 |
| `shop_id` | bigint(21) | Shop ID (PURCHASE_ORDER only) · 可空 |
| `name` | varchar(100) | Name (snapshot) |
| `description` | varchar(500) | Description (snapshot) · 可空 |
| `unit_price` | decimal(10, 2) | Unit price (snapshot) |
| `currency` | varchar(3) | Currency code, default AED |
| `cover_url` | varchar(255) | Cover image URL (snapshot) · 可空 |
| `quantity` | int | Quantity |
| `subtotal` | decimal(10, 2) | Subtotal = unitPrice * quantity |
| `category` | varchar(30) | Category (PAYMENT_ITEM only): UTILITIES, DINING, GIFT, RENT, TRIP, OTHER · 可空 |
| `created_at` | timestamp | Creation time (UTC) |

## 主键 / 索引
- 主键:`id`
- `idx_shop_id`:shop_id
- `idx_template_id`:template_id

## 校验点(QA 关注)
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
