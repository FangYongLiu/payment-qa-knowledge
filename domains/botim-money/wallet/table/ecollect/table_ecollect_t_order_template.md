---
id: tbl_ecollect_t_order_template
object_type: Table
name: Order template table (t_order_template)
aliases: [t_order_template, ecollect.t_order_template]
domain: wallet
status: active
owner: qianlong.wang
reviewer: qianlong.wang
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: ecollect schema DDL
tags: [wallet, ecollect]
sensitivity: normal
related_services: []
---

# Order template table (t_order_template)

## 用途
物理表 `ecollect.t_order_template`,主键 `id`。Order template table。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint(21) | Template ID, generated by IdGenerator |
| `owner_id` | varchar(8) | Owner Botim UID |
| `shop_id` | bigint(21) | Shop ID · 可空 |
| `type` | varchar(20) | Template type: PRODUCT_ORDER, PAYMENT_REQUEST, PURCHASE_ORDER |
| `title` | varchar(100) | Template title, max 100 characters |
| `description` | varchar(500) | Template description, max 500 characters · 可空 |
| `total_amount` | decimal(10, 2) | Total amount, sum of all items |
| `currency` | varchar(3) | Currency code, default AED |
| `collected_amount` | decimal(10, 2) | Collected amount (redundant statistic, atomically updated) |
| `order_structure` | varchar(20) | Order structure: INDEPENDENT, SPLIT_TOGETHER |
| `split_mode` | varchar(20) | Split mode: SINGLE_PAYER, EQUAL_SPLIT |
| `split_count` | int | Split count for EQUAL_SPLIT mode, max 20 (BR1) · 可空 |
| `per_person_amount` | decimal(10, 2) | Per person amount = totalAmount / splitCount · 可空 |
| `show_total_amount` | tinyint(1) | Privacy: show total amount, 0=No, 1=Yes |
| `show_participant_detail` | tinyint(1) | Privacy: show participant detail, 0=No, 1=Yes |
| `status` | varchar(20) | Status: DRAFT, ACTIVE, COMPLETED, EXPIRED, CLOSED |
| `paid_count` | int | Paid count (redundant statistic, atomically updated) |
| `create_by` | varchar(8) | Creator Botim UID · 可空 |
| `update_by` | varchar(8) | Updater UID · 可空 |
| `deadline` | timestamp | Deadline (UTC), default 30 days, max 30 days (BR3) · 可空 |
| `created_at` | timestamp | Creation time (UTC) |
| `updated_at` | timestamp | Update time (UTC) · 可空 |
| `completed_at` | timestamp | Completion time (UTC, when status becomes COMPLETED) · 可空 |
| `closed_at` | timestamp | Close time (UTC, when status becomes CLOSED) · 可空 |
| `closed_by` | varchar(8) | Closer UID · 可空 |
| `deleted` | tinyint(1) | Logical deletion flag, 0=active, 1=deleted |
| `deleted_at` | timestamp | Deletion time (UTC) · 可空 |
| `deleted_by` | varchar(8) | Deleter UID · 可空 |

## 主键 / 索引
- 主键:`id`
- `idx_deadline`:deadline
- `idx_owner_id`:owner_id
- `idx_shop_id`:shop_id

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
