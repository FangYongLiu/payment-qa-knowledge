---
id: tbl_ecollect_t_order_template_share
object_type: Table
name: Order template share record table (t_order_template_share)
aliases: [t_order_template_share, ecollect.t_order_template_share]
domain: wallet
status: active
owner: qianlong.wang
reviewer: qianlong.wang
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: ecollect schema DDL
tags: [wallet, ecollect]
sensitivity: normal
related_services: []
---

# Order template share record table (t_order_template_share)

## 用途
物理表 `ecollect.t_order_template_share`,主键 `id`。Order template share record table。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint(21) | Share record ID, generated by IdGenerator |
| `template_id` | bigint(21) | Template ID (FK -> t_order_template) |
| `sharer_id` | varchar(8) | Sharer ID (Botim User ID) |
| `share_token` | varchar(64) | Unique share token for tracking (UK) |
| `channel` | varchar(20) | Share channel: BOTIM_CONTACT, BOTIM_GROUP, PHONE, WECHAT, WHATSAPP, QR_CODE, LINK |
| `entry_type` | varchar(20) | Entry type: PRIMARY (auto add to pending list), SECONDARY (user must access) |
| `shared_at` | timestamp | Share time (UTC) |
| `view_count` | int | View count (number of times link accessed) |
| `order_count` | int | Number of orders created via this share |

## 主键 / 索引
- 主键:`id`
- `uk_share_token`:share_token (UNIQUE)
- `idx_template_id`:template_id

## 校验点(QA 关注)
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
