---
id: tbl_ecollect_t_product
object_type: Table
name: Product table (t_product)
aliases: [t_product, ecollect.t_product]
domain: wallet
status: active
owner: qianlong.wang
reviewer: qianlong.wang
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: ecollect schema DDL
tags: [wallet, ecollect]
sensitivity: normal
related_services: []
---

# Product table (t_product)

## 用途
物理表 `ecollect.t_product`,主键 `id`。Product table。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint(21) | Product ID, generated by IdGenerator |
| `owner_id` | varchar(8) | Product owner ID (Botim User ID) |
| `shop_id` | bigint(21) | Shop ID (FK -> t_shop.id) · 可空 |
| `name` | varchar(50) | Product name, max 50 characters |
| `unit_price` | decimal(10, 2) | Unit price (0.01-5000 AED for personal users) |
| `currency` | varchar(3) | Currency code, default AED |
| `cover_url` | varchar(255) | Cover image URL (first attachment, dynamically generated) · 可空 |
| `description` | varchar(500) | Product description, max 500 characters · 可空 |
| `status` | varchar(20) | Status: ACTIVE, INACTIVE |
| `created_at` | timestamp | Creation time (UTC) |
| `updated_at` | timestamp | Update time (UTC) · 可空 |

## 主键 / 索引
- 主键:`id`
- `idx_owner_id`:owner_id
- `idx_shop_id`:shop_id

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
