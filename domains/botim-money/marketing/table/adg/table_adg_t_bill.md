---
id: tbl_adg_t_bill
object_type: Table
name: Bill Table (t_bill)
aliases: [t_bill, adg.t_bill]
domain: marketing
status: active
owner: wujiang.ding
reviewer: wujiang.ding
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: adg schema DDL
tags: [marketing, adg]
sensitivity: normal
related_services: []
---

# Bill Table (t_bill)

## 用途
物理表 `adg.t_bill`,主键 `id`。Bill Table。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | Auto generated bill id · 可空 |
| `client_bill_id` | varchar(63) | Client-specific bill identifier · 可空 |
| `app_code` | varchar(31) | Client application code |
| `user_id` | varchar(63) | User id for the loan provided by Client application |
| `dda_ref_no` | varchar(127) | Unique dda refference number Generated by system as {app_code}{loan_id}{mandate_count} |
| `loan_id` | varchar(63) | Loan ID choosen by client appication. Expected Integer or uuid |
| `installment_id` | varchar(63) | Installment id provided by client appication. Expected Integer or uuid |
| `claimed_amount` | decimal(10, 4) | Claimed ammount for the bill. Provided by Client application |
| `due_date` | date | Bill Due date. provided by client application |
| `remarks` | varchar(255) | Any comments on the bill · 可空 |
| `status` | varchar(31) | Bill Status. Generated and updated by the system |
| `created_at` | timestamp | Entry create time |
| `updated_at` | timestamp | Last change time |
| `dda_ref_no_bill_serial` | int | Number of bill aginist a mandate. Counting is managed by the system |

## 主键 / 索引
- 主键:`id`
- `uk_dda_ref_no_serial`:dda_ref_no, dda_ref_no_bill_serial (UNIQUE)
- `idx_app_code_loan_id`:app_code, loan_id
- `idx_app_code_user_id`:app_code, user_id
- `idx_due_date`:due_date

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
