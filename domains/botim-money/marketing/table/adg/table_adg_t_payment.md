---
id: tbl_adg_t_payment
object_type: Table
name: Payment submitted againist each bill (t_payment)
aliases: [t_payment, adg.t_payment]
domain: marketing
status: active
owner: wujiang.ding
reviewer: wujiang.ding
last_reviewed_at: '2026-07-02'
source_type: DB DDL
source_ref: adg schema DDL
tags: [marketing, adg]
sensitivity: normal
related_services: []
---

# Payment submitted againist each bill (t_payment)

## 用途
物理表 `adg.t_payment`,主键 `id`。Payment submitted againist each bill。业务语义细节**待补**(表结构来自 DDL)。

## 关联关系
- **所属服务**:待补。
- **谁读写它**:相关服务 / 接口(由对方文档 `related_tables` 声明)。
- **哪些场景校验它**:待补。

## 关键列
| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | Auto generated id · 可空 |
| `user_id` | varchar(63) | User id for the loan provided by Client application |
| `app_code` | varchar(31) | Client application code |
| `payment_file_id` | varchar(127) | Id for each payment file generated by uaedds |
| `payment_id` | varchar(127) | id for each payment generated by uaedds |
| `dda_ref_no` | varchar(127) | Unique dda refference number Generated by system as {app_code}{loan_id}{mandate_count} |
| `payment_ref_no` | varchar(63) | Unique reffernce for each paymnet for a mandate choosen by the system |
| `claimed_amount` | decimal(10, 4) | Same as bill ammount for a bill |
| `due_date` | date | Bill Due date. provided by client application |
| `remarks` | varchar(255) | Comment on the payment · 可空 |
| `status` | varchar(31) | Payment status provided by uae dds |
| `ft_ref_no` | varchar(127) | Payment refference from bank when successful · 可空 |
| `payment_accept_time` | timestamp | when system verify payment as paid · 可空 |
| `reason_code` | varchar(7) | Payment Status reason code returned by uae dds · 可空 |
| `errors` | text | Error list returned by uaedds as an object. each field in object is an array of string · 可空 |
| `bill_id` | bigint | Payment for the bill. refference to bill table |
| `created_at` | timestamp | Create time |
| `updated_at` | timestamp | Last change time |
| `reconciliation_status` | varchar(20) | Reconciliation status: SUCCESS, FAILED, NULL=not reconciled · 可空 |
| `reason_failed_reconciliation` | varchar(255) | Reason for failed reconciliation · 可空 |

## 主键 / 索引
- 主键:`id`
- `idx_app_code`:app_code
- `idx_payment_created_at`:created_at
- `idx_payment_payment_file_id`:payment_file_id

## 校验点(QA 关注)
- **状态字段**:`status` 合法枚举与流转规则需结合代码/业务文档核对(**待补**)。
- **金额精度**:decimal 比较用容差(< 0.01);amount 与 currency 需一致。
- 业务语义、状态枚举、跨表关联**待补**(需结合代码或业务文档)。
